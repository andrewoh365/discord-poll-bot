from config import run_bot, client
import events  # Ensure this imports the event handlers
import poll  # Ensure this imports the poll logic
import tasks  # Ensure this imports the tasks

# Run the bot
run_bot()
